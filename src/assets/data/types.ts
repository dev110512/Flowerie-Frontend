export type CityType = {
  city: string;
  distance: string;
  img: string;
};

export type VenueType = {
  venue: string;
  img: string;
};

export type NavLinksType = {
  label: string;
  href: string;
};
